#!/bin/sh

# Run linter
echo "🔍 Running linter..."
pnpm lint || exit 1

# Run type checking
echo "📋 Running type check..."
pnpm typecheck || exit 1

# Run tests
echo "🧪 Running tests..."
pnpm test || exit 1

echo "✅ All pre-commit checks passed!"
