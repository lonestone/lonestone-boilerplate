commit-msg:
  jobs:
    - name: commitlint
      run: pnpm exec commitlint --edit "{1}"

pre-push:
  follow: true
  jobs:
    - name: quality
      group:
        piped: true
        jobs:
          - name: lint
            run: pnpm lint
          - name: typecheck
            run: pnpm typecheck
